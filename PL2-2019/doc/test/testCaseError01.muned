MODULE errorSemanticoUno;

  (* pruebas con operaciones *)
(* se tiene que detectar error semántico al intentar usar OR con un booleano y un entero*)

  CONST A = 10;
	B = 20;

  VAR mult : INTEGER;
	a1:INTEGER;
	b1:INTEGER;
	bol1 : BOOLEAN;
	bol2 : BOOLEAN;
	bol3 : BOOLEAN;
  BEGIN

    a1:=A;
    b1:=2;

    mult:=a1*b1;

   
    
	bol1:=TRUE;
	bol2:=FALSE;
	(*a continuación se tiene que detectar el error semántico*)
	bol3:=(bol1 OR a1);
	    
  END errorSemanticoUno;
