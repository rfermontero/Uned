#define TRUE 1;
int array[3][2];

void nada() {
 int a = array;//Asignacion a array, error.
 printc("mundo");
}

void nada2 () {
 printc("mundo");
}

int sumar (int &x, int &y) {
 printc("mundo");
 return x + y;
}

int sumara(int &x, int &y){
 printc("mundoa");
 return x + y;
}

void main () {
 printc("hola");
 printc("mundo");

}