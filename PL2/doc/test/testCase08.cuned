/* Precedencia de operadores */

void main() {
	int a;
        int x;

	printc("PRECEDENCIA DE OPERADORES");

	x=5;
      	a= ((x+1) < 8) && 1;

	printc ("true:");
      	if (a && 1) {
		printc(" true");
	}
        
      	else {
        	printc(" false");
	}
}